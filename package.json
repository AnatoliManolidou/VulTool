{
  "name": "cti-vulnerability-remediation",
  "version": "1.0.0",
  "description": "Autonomous CTI vulnerability detection and patch remediation pipeline.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "postbuild": "cp node_modules/web-tree-sitter/tree-sitter.wasm dist/ && cp wasm/tree-sitter-javascript.wasm dist/",
    "test": "jest"
  },
  "dependencies": {
    "@actions/cache": "^3.3.0",
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "semver": "^7.8.5",
    "web-tree-sitter": "^0.20.8"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/semver": "^7.7.1",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  }
}
